# https://leetcode.com/problems/count-negative-numbers-in-a-sorted-matrix/

# @param {Integer[][]} grid
# @return {Integer}
def count_negatives(grid)
  return 0 if grid.size == 0
  
  r_size = grid.size - 1
  c_size = grid.first.size - 1
  
  r = 0
  c = c_size
  
  while r < grid.size && c >= 0
      if grid[i][j] < 0 && grid[i][]
  end
      
end